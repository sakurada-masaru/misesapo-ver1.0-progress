# Files excluded from gcloud build context
# If this file exists, gcloud will NOT fallback to .gitignore.

# Git metadata
.git/
.gitignore
.gcloudignore

# CI configs (not needed for container build)
.github/

# OS/editor junk
**/.DS_Store
**/*.swp
**/*.swo

# Python cache
**/__pycache__/
**/*.pyc
**/*.pyo

# Node
node_modules/

# Keep build outputs and site sources included
# (Do NOT exclude public/ — Dockerfile copies it.)
# public/
# src/
# nginx/
# scripts/
